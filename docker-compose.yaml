version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - GOOGLE_CREDENTIALS=${GOOGLE_CREDENTIALS}
      - SPREADSHEET_ID=${SPREADSHEET_ID}